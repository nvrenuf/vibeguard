# Audit Pack

This folder defines the audit pack format used by VibeGuard.

- `templates/manifest.json` — manifest schema template (illustrative)
- `spec/AUDIT_PACK.md` — requirements for contents

The generated audit pack is written to an output directory (local-first).
